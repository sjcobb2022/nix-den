# Common secrets shared across hosts
# Encrypt this file with: sops -e -i secrets/common.yaml
# Or edit encrypted: sops secrets/common.yaml

# Example secrets (uncomment and fill in after encrypting):
# github-token: ghp_...
# api-key: ...
