# Slaptop host secrets
# Encrypt this file with: sops -e -i secrets/slaptop.yaml
# Or edit encrypted: sops secrets/slaptop.yaml

# Example secrets (uncomment and fill in after encrypting):
# user-password: $6$rounds=...  # mkpasswd -m sha-512
# wifi-home: "your-wifi-password"
